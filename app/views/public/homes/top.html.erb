<div class="row">      
<div class="container">
<div class="row">
    <!--ここからサイドバー-->
<table class='table table-hover table-inverse col-md-2 '>
   <thead><tr>
   <th>ジャンル検索</th>
  </tr></thead>
   <% @genres.each do |genre| %>
   <tr><td><%= genre.name %></td></tr>
   <% end %>
   </table>
   <!--ここまでサイドバー-->
<div class="col-md-7">
  <h5 class="offset-2 ">ようこそ、ながのCAKEへ！<br>
  このサイトは、ケーキ販売のECサイトになります。<br>
  会員でない方も商品の閲覧はできますが、<br>
  購入には会員登録が必要になります。</h5>
   <h3 class=" offset-md-3">新着商品</h3>
          <div class="row">
          <% @items.each do |item| %>
          <div class="col-md-3">
          <%= attachment_image_tag item,:image,:fill,500,350, fallback: "no-image.jpg",size:'150x100' %><br>
          <%= link_to item.name,item_path(item) %><br>
          ¥<%= item.price %>
          </div>
          <% end %>
    </div></div></div></div></div>
 