  
   <h2 class="text-center">商品一覧<small>(全 <%= @items.count %>件)</small></h2>
     <div class="col-md-9">
          <div class="row">
          <% @items.each do |item| %>
          <div class="col-md-3">
          <%= attachment_image_tag item,:image,:fill,500,350, fallback: "no-image.jpg",size:'150x100' %><br>
          <%= link_to item.name,item_path(item) %><br>
          ¥<%= item.price %>
          </div>
          <% end %>
          </div>
           </div>
    