<div class="row">
<div class="col-sm-8 px-sm-0 offset-sm-1">
<h1>商品新規投稿</h1>
<div class="col-sm-10 px-sm-0 mx-auto md-5">
<%= form_with url:admin_items_path, model:@item, local:true do |f| %>
<table>
  <tr>
  <th>商品画像</th>
  <th><%= f.attachment_field :image %></th></tr>
  <tr>
  <th>商品名</th>
  <th><%= f.text_field :name , :size=>"30"%></th></tr>
  <tr>
  <th>商品説明</th>
  <th><%= f.text_area :introduction , :size=>"33x5"%></th></tr>
  <tr>
  <th>ジャンル</th>
  <th><%= f.collection_select :genre_id, Genre.all, :id, :name, include_blank: "-- 選択してください --"  %></th></tr>
  <tr>
  <th>税抜価格</th>
  <th><%= f.text_field :price , :size=>"30" %></th></tr>
  <tr>
  <th>販売ステータス</th>
  <th><%= f.radio_button :is_active, true %> 販売中
  <%= f.radio_button :is_active, false %> 販売停止中</th></tr>
  <tr>
  <th>
  <%= f.submit '新規登録', class: " offset-sm-8 btn btn-outline-success" %>
  </th></tr>
<% end %>
</div></table ></div></div>